# Geyser-Standalone Configuration for PAK MC SERVER
# This runs as a background bridge between Bedrock and Java players.

bedrock:
  address: 0.0.0.0
  port: 19132
  clone-remote-port: false
  motd1: "PAK MC SERVER"
  motd2: "Bedrock Bridge"

remote:
  address: 127.0.0.1
  port: 25565
  auth-type: offline

# Enable Floodgate integration if needed later
# auth-type: floodgate

# Geyser tries to do some auto-magic, but we'll be explicit.
# See more: https://wiki.geysermc.org/geyser/setup/
